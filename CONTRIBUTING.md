# Contributing

欢迎提交可复现的代码修复、数据审计、评测和受控实验。请遵守以下约定：

1. 不提交基础模型权重、checkpoint、访问令牌、SSH 配置或未经许可核验的原始数据。
2. 新数据须记录来源、固定版本、许可、筛选规则、切分方法和内容哈希；训练集与冻结测试集不得混用。
3. 新训练实验使用独立输出目录，保留失败日志和 checkpoint，不覆盖或删除负面结果。
4. 指标必须同时说明数据规模、生成协议、随机种子和不确定性；自动指标不能冒充人工评价。
5. 改动训练逻辑时补充最小回归测试，并运行 `python -m pytest -q` 与 `python -m ruff check .`。

提交 PR 时请在说明中列出：研究问题、与基线的唯一主要差异、预注册门槛、实际结果、失败模式和复现命令。
